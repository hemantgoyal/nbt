<%= form_for(@maillinglist) do |f| %>
  <% if @maillinglist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@maillinglist.errors.count, "error") %> prohibited this maillinglist from being saved:</h2>

      <ul>
      <% @maillinglist.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
